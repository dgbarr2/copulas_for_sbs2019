import React from "react"


const productCopula = (m,n) => {
    // d is an array of m arrays of n observations.
    // E.g. m assets with n sample returns
    console.log("d = ", d)
    return d
}

export default d